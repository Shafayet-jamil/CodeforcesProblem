#include<bits/stdc++.h>
using namespace std;

int main()
{
	int test;
	cin>>test;
	while(test--)
	{
     int a, b, c, d;
     cin>>a>>b>>c>>d;
     if(abs(a)==abs(b)and abs(a)==abs(d) and abs(c) == abs(a)and abs(b) and abs(c) and abs(b)and abs(d) and abs(c) == abs(d) ){
		cout<<"YES"<<endl;
     }
     else{
		cout<<"NO"<<endl;
     }

	}

return 0;
}
